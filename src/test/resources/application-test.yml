spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    username: test
    password: test
  main:
    lazy-initialization: true
app:
  bootstrap:
    with:
      data: true
  demo-data:
    username: demo
