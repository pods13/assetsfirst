spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  datasource:
    username: test
    password: test
  main:
    lazy-initialization: true
  cache:
    type: none
app:
  bootstrap:
    with:
      data: true
  demo-data:
    username: demo
